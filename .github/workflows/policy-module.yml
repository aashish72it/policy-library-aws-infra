name: policy-module

on:
  push:
    branches: [ "main" ]

permissions:
  contents: write
  pull-requests: write

jobs:
  policy-module:
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@v4
        with:
          config-file: .github/.policy-module-config.json
          manifest-file: .github/.policy-module-manifest.json
          token: ${{ secrets.CICD_PAT }}