import "tfplan/v2" as tfplan

vers = tfplan.resource_changes["aws_s3_bucket_versioning"]

enabled = func(v) {
  v.change.after.versioning_configuration.status is "Enabled"
}

main = rule {
  all vers as v { enabled(v) }
}